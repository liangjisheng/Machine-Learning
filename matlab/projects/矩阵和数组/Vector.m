d=linspace(0,2*pi,10);  % 0-2*pi 上等分插值10个点
a=rand(1,6);  % 创建1行6列范围在0-1之间的矩阵
a1=[1:6];
a2=a.*a1;   % 对应元素相乘
a3=a./a2;
a4=[2,3,2,3,2,3];
a5=a1.^a4;
a6=a1.^4;   % 每个元素的4次方
b=rand(6,1);  % 创建6行1列范围在0-1之间的矩阵
c=rand(3,4);  % 创建3行4列范围在0-1之间的矩阵
e=fix(rand(3,3)*30); % 3*3的0-30之间的随机整数
x=[-3:3]; 
y1=x(abs(x)>1); % 将x中元素绝对值大于1的元素选出来
y2=x([1,1,1,1]); % y2=[-3,-3,-3,-3]
x(abs(x)>1)=[]; % 将x中元素绝对值大于1的元素删除
f=dot(a,b); %相当于a'*b
c1=[1,2,3];
c2=[4,5,6];
g=cross(c1,c2);
norm(a);    % a的模
