z=zeros(3,4); % 3*4的0矩阵
x=ones(4);  % 4*4 的元素全为1的矩阵
y=rand(5);  % 5*5 的0-1之间的随机矩阵
A=fix(100*rand(5)); % 取整
A(3,3)=0;
A(6)=100; %列优先排列
e=eye(5); % 5*5的单位阵
m=magic(5); % 魔方阵

a=1:25;
a=reshape(a,5,5);
a(3,1)=3;   % or a(3)=3;
a(3,:)
a(:,4)
a(end,:) % 最后一行
a([1,3,5],[2,4])
