a=[1,2,3;4,5,6;7,8,0];
b=[1;2;3];
c=a*b;
d=[-1;0;2];
f=pi*d;
a0=a^2;
a1=max(a); % 求矩阵A每列的最大元素
a2=min(a); %求矩阵A每列的最小元素
a3=mean(a); %求矩阵A列元素的平均值
a4=median(a); % 求矩阵A列元素的中值
a5=std(a); %求矩阵A元素的标准差
a6=sum(a); %求矩阵A各列元素的和
a7=prod(a); % 求矩阵A各列元素的积